# Lougha Landing Employee

## EasyPanel
- **Root Directory:** `. ` (racine du repo)
- **Dockerfile Path:** `Dockerfile.employee`
- **Port:** `3000`

## Variables
Voir `env.EXEMPLE`

```bash
cp env.EXEMPLE .env
npm install && npm run dev
```

## Docker
```bash
docker compose up --build
```
